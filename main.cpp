#include <iostream>
#include "graph.h"

int main() {
    /*DSU<int> dsu;

    dsu.make_set(1);
    dsu.make_set(2);
    dsu.make_set(3);

    auto sets = dsu.get_sets();

    for (auto& vec : sets) {
        for (auto& v : vec) {
            std::cout << v << ' ';
        }
        std::cout << std::endl;
    }

    dsu.union_sets(1, 2);
    dsu.make_set(4);
    sets = dsu.get_sets();

    for (auto& vec : sets) {
        for (auto& v : vec) {
            std::cout << v << ' ';
        }
        std::cout << std::endl;
    }

    dsu.union_sets(4, 3);
    sets = dsu.get_sets();

    for (auto& vec : sets) {
        for (auto& v : vec) {
            std::cout << v << ' ';
        }
        std::cout << std::endl;
    }*/

    /*Kosaraju k;

    auto sets = k(
            {
                    {1, {2}},
                    {2, {}},
                    {3, {1}}
            }
            );

    for (auto& vec : sets) {
        for (auto& v : vec) {
            std::cout << v << ' ';
        }
        std::cout << std::endl;
    }*/

    return 0;
}